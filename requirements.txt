# --- API (app.py) ---
Flask>=3.0.0,<4
Werkzeug>=3.0.0,<4

# --- Vision & tableaux ---
opencv-python>=4.9.0.80
numpy>=1.26.0,<2.0
Pillow>=10.2.0

# --- OCR ---
pytesseract>=0.3.10
# PaddleOCR (utilisé par ocr_manager, CIN, carte grise). Sur certaines plateformes,
# installer paddlepaddle depuis la doc officielle si pip échoue.
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# --- Tests client HTTP ---
requests>=2.31.0

# --- Scripts outils / génération de templates (config/build_*.py, utils/ocr_utils) ---
easyocr>=1.7.0
